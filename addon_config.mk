meta:
	ADDON_NAME = ofxMyoWebSockets
	ADDON_DESCRIPTION = openframeworks addon for Thalmic Myo Armbands over the native WebSocket interface
	ADDON_AUTHOR = Matt Felsen
	ADDON_TAGS = "myo" "thalmic" "armband" "gesture" "websocket"
	ADDON_URL = https://github.com/mattfelsen/ofxMyoWebSockets

common:
	# dependencies with other addons, a list of them separated by spaces 
	# or use += in several lines
	ADDON_DEPENDENCIES = ofxJSON ofxLibwebsockets